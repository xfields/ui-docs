/* eslint key-spacing:0 */
import webpack from 'webpack'
import config from '../config'
import _debug from 'debug'

const debug = _debug('app:webpack:test')

export default webpackConfig => {
  return webpackConfig
}
